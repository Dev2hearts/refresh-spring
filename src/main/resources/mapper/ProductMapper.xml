<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.refresh.product.ProductMapper">
    <insert id="insPdt">
        INSERT INTO t_product
        SET iplan = #{iplan}
        , icate = #{icate}
        , iunit = #{iunit}
        , w_iuser = #{wIuser}
        , nm = #{nm}
        , cnt = #{cnt}
    </insert>

    <select id="selPdt">

    </select>

</mapper>